import React from 'react';

const Hero: React.FC = () =>{
    return (
        <div className="hero">
            <h5 className="hero__text">REACT + TYPESCRIPT + SCSS + WEBPACK</h5>
        </div>
    );
};

export default Hero;